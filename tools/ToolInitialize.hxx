#ifndef ToolInitialize_hxx_seen
#define ToolInitialize_hxx_seen

namespace Cube {
    namespace Tool {
        // Fill internal fields for the other functions.  This is a KLUDGE
        // zone.
        void Initialize();
    }
}
#endif

// Local Variables:
// mode:c++
// c-basic-offset:4
// compile-command:"$(git rev-parse --show-toplevel)/build/cube-build.sh force"
// End:
